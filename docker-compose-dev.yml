version: '3.8'
services:
  api:
    volumes:
      - ./data:/code/data
      - ./src:/code/src
    entrypoint: 
      - "uvicorn"
      - "src.api.main:app"
      - "--host"
      - "0.0.0.0"
      - "--port"
      - "80"
      - "--reload"