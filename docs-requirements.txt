# Top-level dependencies for building the docs and executing the notebooks.
# Kept deliberately minimal: transitive versions are left to the installer.
# Add a version constraint here only when one is actually required (and say why).

# --- Build toolchain ---
jupyter-book<2          # 2.x is a breaking rewrite incompatible with this _config.yml / _toc.yml

# --- Notebook libraries ---
gql[requests]           # GraphQL client; modern gql/graphql-core (>=3.2.11) parses the server schema
matplotlib
numpy
pandas
scipy
seaborn
requests
pyarrow
xarray
zarr
s3fs                    # s3:// access for level2 zarr stores
s5cmd                   # CLI used in the bulk download notebook
