name: CI
on: [ push, pull_request ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: |
          pip install pytest
          pytest
    
      - name: Shutdown
        run: |
          docker-compose down





