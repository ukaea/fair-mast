name: mast-book
channels:
  - conda-forge
  - intake
dependencies:
  - python=3.11
  - psycopg2
  - intake-sql
  - pip
  - pip:
      - xarray-datatree==0.0.12
      - pandas
      - intake
      - matplotlib
      - requests
      - zarr
      - xarray
      - pyspark
      - dask
      - jupyter-book
      - ghp-import
      - pyyaml
      - s3fs
      - scikit-image
      - gql
      - ipykernel
      - requests_toolbelt
      - intake-xarray
  
